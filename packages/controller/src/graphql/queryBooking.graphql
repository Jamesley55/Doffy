query QueryBooking($serviceId: String!, $date: String!) {
  QueryBooking(serviceId: $serviceId, date: $date) {
    startService
    endService
    price
    taxes
    depositAmount
    isRefund
    transactionFee
    Total
  }
}
