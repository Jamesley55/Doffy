overwrite: true
schema: "./src/Schemas/schemas.graphql"
generates:
  src/types/graphql-hooks.ts:
    hooks:
    plugins:
      - "typescript"
      - typescript-operations
      - "typescript-react-apollo"
    config:
      withHOC: false
      withComponent: false
      withHooks: true
